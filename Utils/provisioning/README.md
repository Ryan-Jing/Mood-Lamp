# provisioning

The Python BLE provisioning app lives here.

Purpose: connect to a Mood Lamp over BLE and hand it the WiFi credentials (SSID +
password) and any first-time config, so the device can join the network and reach
the server. See [`Docs/architecture.md`](../../Docs/architecture.md) §2.5 for the
GATT layout this app targets.

Not implemented yet — placeholder so the folder location is fixed under `Utils/`.
